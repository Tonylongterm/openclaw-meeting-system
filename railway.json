{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "variables": {
        "NIXPACKS_NO_CACHE": "1"
      }
    }
  },
  "deploy": {
    "startCommand": "sh -c 'for d in \"${RAILWAY_WORKDIR:-}\" /app \"$PWD\"; do if [ -n \"$d\" ] && [ -f \"$d/server.py\" ]; then cd \"$d\"; break; fi; done; exec gunicorn server:app --bind 0.0.0.0:$PORT --timeout 120 --worker-class gthread --workers 1 --threads 4'",
    "healthcheckPath": "/api/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
